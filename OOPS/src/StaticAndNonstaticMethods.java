public class StaticAndNonstaticMethods {
    //Static is for class, it belongs to class
    // Non-static is for objects
// Static methods are created with classes and when class is running also our static method is running.
    //but non-static method are running when objects are running.

}
